import tkinter as tk
import tkinter.ttk as ttk
import os
from symtable import Class

class ScriptManager(tk.Tk):
    def __init__(self):

    def button:
        #кнопка закрытия окна
        btn_close = tk.Button(root, text="Закрыть окно", command=root.destroy)
        btn_close.pack()

        #кнопка открытия папки (\\nas\Distrib\script\.manual_scripts)
        btn_open_folder = tk.Button(root, text="Открыть папку со скриптами")
    def _main_:
        root = tk.Tk()
        root.title("Script manager")
        root.geometry("1200x800")
        root.mainloop()
